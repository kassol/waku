# 隔离 Waku，复用原生编码助手配置

用户于 2026-09-06 确认，测试 App 必须隔离 Waku 自身的安装身份、进程、daemon、配置、数据库、工作目录和辅助程序；允许 Claude/Codex 共用现有原生配置及会话目录，以沿用已有登录与服务配置。

测试产生的原生 CLI 会话可与现有环境共存，这是隔离边界的明确例外。测试使用新建会话和专用工作区，不主动续写或清理原有会话，不为测试擅自修改共用服务配置。原版 Waku 的安装、运行进程和自有数据保持不动。

首次启动前必须补齐 Waku 自有资源的隔离缺口。现有普通 Release 包仍使用原版身份，不能作为隔离测试包；需要优化构建测试时，也必须保留测试身份与数据边界。

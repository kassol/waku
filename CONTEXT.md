# Waku

Waku 管理编码助手会话。管家功能允许用户通过一个会话委派和查看其他会话的工作。

## Language

**管家会话**：用户用来委派工作并管理直属子会话的会话。
_Avoid_: 固定流水线

**子会话**：由另一个会话创建、承担临时委派工作的独立会话。负责分担上下文和并行执行，由管家统一接管结果与对用户的交互。
_Avoid_: harness 内部 subagent

**直属子会话**：由当前管家会话直接创建的子会话。更深层的后代由各自的父会话管理。

**子会话归档**：子会话完成生命周期后退出日常会话列表，完整执行记录继续保留，主会话保存摘要与追溯入口。

**管家代决**：管家在用户目标和已有授权内处理子会话的问题；超出授权的事项由管家在主会话询问用户。

上述子会话分层为已确认的产品方向，实施状态见 [ADR 0005](docs/adr/0005-transient-child-sessions-and-manager-decisions.md)。

**会话历史**：用户可阅读的会话内容，保留到用户主动删除。
_Avoid_: 内部事件日志

**任务集成分支**：承接一次委派任务中各子会话成果、用于整体验收的分支。

**即时交流**：用户在执行继续进行时，通过独立入口了解任务或讨论想法。仅在用户明确要求改变计划时向执行中的管家投递指令。

即时交流是关联来源任务的独立只读讨论。daemon 在 `consultations` 表保留完整问答，提问和加载不改变父任务的轮次、等待和计划。用户通过 `Consult` / `LoadConsultation` 访问，管家 MCP 不暴露这些命令。交流使用已配置 Claude 的原生安全模式及空工具集；能力不满足时拒绝启动，界面明确显示 provider。每次提问仅输入来源任务和同项目直系子任务的有界记录，并显示记录时间。来源删除不自动删除交流历史。

用户单独点击“交给管家执行”时，`ExecuteConsultation` 在同一交流记录中保存原文、固定上下文、投递标识和待处理子任务关联，再复用可靠输入投递到来源任务。加载交流时读取 daemon 的最新投递状态；相同标识重试不重建上下文。新方向结果待核实时，暂停该轮旧等待的自动回调；明确未送达允许旧等待继续，确认收到后由新计划重新登记必要等待。执行入口仅开放给用户，管家 MCP 的直属子会话权限保持不变。

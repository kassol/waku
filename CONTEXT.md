# Waku

Waku 管理编码助手会话。管家功能允许用户通过一个会话委派和查看其他会话的工作。

## Language

**管家会话**：用户用来委派工作并管理直属子会话的会话。
_Avoid_: 固定流水线

**子会话**：由另一个会话创建、承担委派工作的独立会话。
_Avoid_: harness 内部 subagent

**直属子会话**：由当前管家会话直接创建的子会话。更深层的后代由各自的父会话管理。

**会话历史**：用户可阅读的会话内容，保留到用户主动删除。
_Avoid_: 内部事件日志

**任务集成分支**：承接一次委派任务中各子会话成果、用于整体验收的分支。

**即时交流**：用户在执行继续进行时，通过独立入口了解任务或讨论想法。仅在用户明确要求改变计划时向执行中的管家投递指令。

即时交流是关联来源任务的独立只读讨论。daemon 在 `consultations` 表保留完整问答，父任务的轮次、等待和计划不变。用户通过 `Consult` / `LoadConsultation` 访问，管家 MCP 不暴露这些命令。交流使用已配置 Claude 的原生安全模式及空工具集；能力不满足时拒绝启动，界面明确显示 provider。每次提问仅输入来源任务和同项目直系子任务的有界记录，并显示记录时间。来源删除不自动删除交流历史。
